# Whitebox Writing Lab 0.4.2

This patch release fixes the independent Sigstore verification step to invoke the pip-installed Sigstore CLI directly. Windows build, Sigstore keyless signing, Fulcio identity verification, and Rekor transparency logging are mandatory before publication.
